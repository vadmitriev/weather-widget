import React from 'react';
import { WeatherWidget } from './widgets';

const App: React.FC = () => {
  return (
    <div className="App">
      <WeatherWidget />
    </div>
  );
};

export default App;
